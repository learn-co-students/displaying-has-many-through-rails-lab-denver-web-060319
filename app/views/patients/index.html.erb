<h1>List of Patient</h1>
<ul>
  <% @patients.each do |patient| %>
  <li>Name: <%= link_to "#{patient.name} Number of Appointments: #{patient.appointments.count}", patient_path(patient) %></li>
  <% end %>
</ul>